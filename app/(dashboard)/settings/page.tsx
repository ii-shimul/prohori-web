import { RoutePlaceholder } from "@/components/shared/route-placeholder";

export default function SettingsPage() {
  return <RoutePlaceholder title="Settings" description="Language and session preferences will appear here after authentication setup." />;
}
